const { response } = require("express");
const { stringify } = require("uuid");
const { Proveedor,DetalleCompra,Egreso,Compra,Producto } = require('../models');


const helperMapear = async(array=[])=>{
    
} 

module.exports ={
    helperMapear
}